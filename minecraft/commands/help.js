module.exports.run = async (bot, args) => {
    return bot.chat(`ตอนนี้มีคำสั่งดังนี้ค่ะ [cal, pos, random, sendjkc, transfer] ตอนใช้อย่าลืมใส่ $ ก่อนด้วยนะคะ`);
}

module.exports.name = ['help', 'h'];
